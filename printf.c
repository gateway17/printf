#include <stdio.h>

/**
int _printf(const char *format, ...)
{
int i = 0;

va_list arg;
/*estructura*/
/**
pt_t types[] = {
{"i", "int", print_int}, 
{NULL, NULL}
};

/*va_start*/
/**
va_start(arg, format);
if (/* condicionales, tenía ejemplos, pero acá van?*/)
/**
return (-1);
*/

int _printf(const char *format, ...)
{
int i = 0;
for (;format[i] != '\0';i++)
{ /* otro while?*/

	write(1,format[i],1);

} 
}
